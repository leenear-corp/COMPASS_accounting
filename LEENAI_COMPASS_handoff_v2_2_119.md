# LEENAI COMPASS 経理 UI — 인수인계 메모 (새 챗 시작용)

> 이 메모는 새 챗을 열 때 `LEENAI_compass_accounting_UI_v2_2_119.html` 파일과 함께 첨부해서
> 대화 시작 메시지에 붙여 넣으시면, 맥락을 다시 설명하지 않고 바로 이어서 작업할 수 있습니다.

---

## 1. 현재 파일

- **파일명**: `LEENAI_compass_accounting_UI_v2_2_119.html`
- 관련 참조 문서: `COMPASS_MAPPING_MASTER.md`, `CLAUDE-D365_Connect_v2_6.md`(§12-4 @odata.bind)

## 2. 화면 구성 (6개 탭, 최종 확정 — 변경 없음)

売上 / Commission / 仕入 / 運送料(ドレー) / 通関 / 海上運賃

## 3. 최근 세션 변경 이력

### v2.2.119 (이번 세션) : ⏳ 取得中オーバーレイ표시 + 정상 시 내부메모 비표시

**배경**: 사용자 요청 2건(스크린샷 첨부) — ① "갱신중에는 「⏳ 更新中…」를 보여주시면서 다른
액션을 취하지 못하도록 해주세요" ② "스크린 샷의 메모는 에러일때만 보여주세요".

**구현 내용**:
- **① 취득 중 화면 전체 차단**: `loadLiveSales()`/`loadLiveDray()` 시작 시 기존 PATCH 갱신과
  동일한 `setBusy(true, {message:'⏳ 更新中…'})`를 호출해 화면 전체를 덮는 `#busy-overlay`를
  표시, `finally`에서 `setBusy(false)`로 해제 — 検索条件 입력창을 포함해 화면 어디도 조작할
  수 없게 됨(기존 PATCH 갱신 시 동작과 동일 UX로 통일).
- **병렬 실행 대응(참조 카운터 도입)**: v2.2.118에서 로그인 시 두 함수를 `Promise.all`로 병렬
  실행하게 되면서, 기존 1:1 show/hide 방식으로는 먼저 끝난 쪽이 아직 진행 중인 다른 쪽의
  오버레이까지 먼저 없애버리는 문제가 생김 → `showBusyOverlay()`/`hideBusyOverlay()`에
  `busyOverlayRefCount` 참조 카운터를 추가, 카운트가 0이 될 때만 실제로 오버레이를 숨김.
  기존 PATCH 갱신(항상 1:1 순차 호출)은 카운트가 정확히 1→0으로 맞아떨어지므로 동작 변경
  없음(하위호환).
- **② 정상 취득 시 내부메모 비표시**: `loadLiveSales()`의 "➡️ Commissionタブにも同時反映され
  ます" 등 3개 안내, `loadLiveDray()`의 "⚠️ B/L No.は今回未対応" / "✅ 仕入タブ 全項目…検証完了"
  안내 — 전부 버전번호·검증상태를 서술하는 내부 개발 메모성 문구라 판단, `$select` 자동제외
  필드나 개별조회 에러 등 실제 문제(`removedFields.length`/`diagnostics.length`)가 있을 때만
  함께 노출하도록 조건화. 정상 취득 시에는 "✅ ライブAPI取得完了: N件 (...)" 한 줄만 표시.
- **적용 범위 확대(사용자 재확인 필요)**: 첨부 스크린샷은 売上タブ(`loadLiveSales`)의 메시지만
  보여주었으나, 동일한 성격(내부 메모)의 문구가 있는 `loadLiveDray()`(運送料タブ) 메시지에도
  일관성을 위해 동일 원칙을 함께 적용함. **의도와 다르면 다음 세션에서 되돌릴 것**(사용자 확인
  대상).
- **범위 밖(변경 없음)**: `$select` 자동제외/개별조회 에러 표시 로직 자체(조건·문구)는 변경
  없음. 라이브 취득 필드·필터·N+1 조회·6개 탭 재렌더링 로직은 전부 변경 없음.
- **문법/전체 검토**: `node --check`로 JS 문법 오류 없음 확인. `showBusyOverlay`/
  `hideBusyOverlay` 함수 재구성 후에도 기존 호출부(`setBusy()`)는 시그니처 변경 없이 그대로
  재사용 가능해 다른 PATCH 갱신 호출부 수정 불필요.
- ⏳ **사용자 실화면 확인 필요**: 로그인 시 오버레이가 뜨는 동안 실제로 다른 조작이 전부
  막히는지, 정상 취득 시 메시지가 의도한 한 줄로만 보이는지 + `loadLiveDray()` 메시지도 동일
  원칙 적용이 맞는지 다음 세션에서 확인 필요.

### v2.2.118 (이전 세션) : 🔄 ログイン時ライブデータ自動取得(全6タブ) — ✅ 확인 완료

- 로그인 성공 시 `loadLiveSales()`/`loadLiveDray()` 병렬 자동 실행. 6개 탭 전부 자동으로
  라이브 데이터 전환. 기존 수동 버튼은 그대로 유지.

### v2.2.117 (이전 세션) : 🌓 ダークモード/ホワイトモード切替機能 — ✅ 확인 완료

- 헤더에 🌙/☀️ 토글 버튼, `body.theme-light` 클래스로 전체 색상 전환. `localStorage`에 저장.

### v2.2.108~116 (이전 세션들) : ✅ 사용자 실화면 확인 완료

- 이전 세션 이력은 `LEENAI_COMPASS_handoff_v2_2_116.md` §3 참조.

## 4. "更新成功 → 체크박스 자동해제" 적용 범위 (변경 없음)

| 탭 | 필드 | 적용 여부 |
|---|---|---|
| 売上 | 着金日 | ✅ v2.2.88 |
| 売上 | 銀行提出日・買取有無 | ✅ v2.2.89 |
| Commission | 支払日 | ✅ v2.2.88 |
| 運送料(ドレー) | 支払日 | ✅ v2.2.91 |
| 仕入 | 支払日 | ✅ v2.2.92 |
| 通関 | 支払日 | ✅ v2.2.95 (실사용 검증 완료) |
| 海上運賃 | 支払日 | ✅ v2.2.101 (실사용 검증 완료) |

## 5. 검증 상태

`node --check`로 JS 문법 오류 없음 확인. HTML 태그 균형 diff는 기존부터 존재하던 불일치
(div/thead/tbody 각 1개, select 2개, v2.2.113 이전부터 존재)와 동일하며, v2.2.119 수정으로
인한 신규 불일치 없음.

**v2.2.119 (取得中오버레이+메모 비표시)**: ⏳ 사용자 실화면 확인 필요.
**v2.2.118 (로그인 시 라이브 데이터 자동취득)**: 사용자 확인 완료.
**v2.2.117 (다크모드/화이트모드)**: 사용자 확인 완료.
**v2.2.108~116**: 사용자 확인 완료(위 §3 참조).

## 6. 지켜야 할 공통 규칙

- **버전 관리**: 파일명에 버전 번호 반영(`v2_2_XX`), 헤더의 `<title>`과 `version-pill`도
  함께 갱신, 상단 `<script>` 주석에 변경이력 항목 추가.
- **브랜딩**: 모든 화면/문서에 `✦ Powered by LEENAI Automation System` 문구 유지.
- **업데이트 시 검증**: 수정 후 전체 HTML 태그 짝, JS 문법(중괄호/백틱/괄호) 재검증.
  (참고: 변경이력 주석 안에 실제 HTML 태그를 예시로 그대로 적으면 태그 밸런스 점검 시
  오탐이 생길 수 있으므로, 예시는 태그 형태가 아닌 서술형으로 표기할 것.)
- **COMPASS Mapping Master 참조**: 필드/엔티티 관련 판단이 필요할 때는
  `COMPASS_MAPPING_MASTER.md`를 먼저 확인.
- **테마(라이트/다크) 원칙(v2.2.117)**: 신규 UI 요소는 `var(--xxx)` 변수 체계를 사용할 것.
- **라이브 데이터 취득 원칙(v2.2.118)**: `loadLiveSales()`(売上/Commission/通関/海上運賃)와
  `loadLiveDray()`(運送料/仕入) 두 함수 구조 재사용.
- **오버레이/메모 표시 원칙(v2.2.119 신규)**:
  - 화면 전체를 막는 장시간 비동기 작업(PATCH 갱신, 라이브 데이터 취득 등)을 새로 추가할 때는
    반드시 `setBusy(true/false, {message:...})`를 호출해 `#busy-overlay`로 화면을 차단할 것.
    여러 작업이 동시에 실행될 수 있는 경우(병렬 처리) `busyOverlayRefCount` 참조 카운터가 이미
    처리해주므로 별도 조치 불필요 — 그냥 setBusy(true)/setBusy(false)를 짝을 맞춰 호출하면 됨.
  - 라이브 취득 결과 메시지에 버전번호·검증상태 등 내부 개발 메모성 문구를 넣을 때는 기본적으로
    비표시하고, 실제 문제(에러/자동제외 필드)가 있을 때만 조건부로 노출하는 것을 기본 원칙으로
    할 것(실무담당자 화면은 간결하게, §화면정리 원칙 v2.2.114와 동일 취지).

## 7. 다음 세션 과제

- **v2.2.119 확인 필요**: ⏳ 로그인 시 오버레이 정상 동작, 정상 취득 시 메시지가 한 줄로만
  나오는지, `loadLiveDray()` 메시지에도 동일 원칙 적용이 맞는지(사용자가 원치 않으면 되돌림).
- **다른 5개 탭 화면정리 필요 여부 검토** — v2.2.114와 동일 원칙.
- **검색조건 순서 ↔ 테이블 열 순서 통일(売上タブ)** — 범위 밖으로 보류된 항목.
- 사용자가 새로운 요청을 주면 그것부터 진행할 것.

### 진행 시 공통 확인사항 (계속 유지)
- 사용자가 COMPASS 実画面 스크린샷을 첨부하면, 그 화면 구조와 기존 구현 일치 여부를
  먼저 확인한 뒤 착수하는 절차를 동일하게 따를 것.
- 사용자가 실화면/DB스키마를 보고 "이 필드 값이 이상하다/이 필드를 참조하나요?"라고
  물으면, 먼저 관련 `$select`·계산 로직에 해당 필드가 실제로 포함돼 있는지 코드로
  직접 확인한 뒤 답할 것(추측 금지, §0 원칙).

---
✦ Powered by LEENAI Automation System
